export default class userModel{
    constructor(name,email,gender,avatar,password){
        this.name = name,
        this.email = email,
        this.gender= gender,
        this.avatar = avatar,
        this.password = password
    }
}